# Bo-Jio

## Setup

1. Clone this repository
2. Start `mysql` server and update configuration in `bojio/config/db.json`
3. Start express server `cd bojio && npm install && npm start`
4. Start React app `cd bojio/client && npm install && npm start`
5. Open `localhost:3000` in browser